# ============================================================
# Lumen — Python backend requirements
# Versions are sensible minimums; pin exact versions for production.
# ============================================================

# ---- Minimal: required to run groq_orchestrator.py ----
#      (Levels 1 and 2 in RUN_GUIDE.md)
groq>=0.11.0
pillow>=10.0.0                     # image handling for the vision step

# ---- RAG knowledge base (Level 3, optional) ----
langchain>=0.2.0
langchain-community>=0.2.0
chromadb>=0.5.0
sentence-transformers>=3.0.0       # local embeddings, no API cost

# ---- API server: needed when you connect the frontend to the backend ----
python-dotenv>=1.0.0               # auto-loads .env into os.environ
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9            # handles image file uploads
pydantic>=2.6.0

# ---- Database / storage / auth (production stack) ----
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0             # PostgreSQL driver
supabase>=2.4.0                    # Supabase client (DB + storage)
python-jose[cryptography]>=3.3.0   # verify Clerk auth tokens (JWT)
httpx>=0.27.0
